PNG sprites for the Visual Studio build (exe output folder is game/).

These files are tracked in git on branch: betaVersionV2-windows

If this folder is missing after clone, run:
  git checkout betaVersionV2-windows
  git pull

Textures are also in assets/textures/ (source copy). Win32 post-build copies them here.
